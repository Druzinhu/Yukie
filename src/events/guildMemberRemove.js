module.exports = async (member, yukie) => {
    /*const user = yukie.users.cache.get(member.id);
    if (!user) {
        yukie.database.update(`Yukie/Users/${member.id}`, { lastUsedDay: 0 });
    }*/
}
